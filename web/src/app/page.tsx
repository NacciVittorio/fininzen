// Placeholder landing for the bare scaffold. Replaced in the routing slice by a
// redirect to /dashboard once the (app) route group exists.
export default function Home() {
    return (
        <main style={{ padding: "2rem", fontFamily: "system-ui" }}>
            <h1>fininzen</h1>
            <p>Next.js scaffold is up.</p>
        </main>
    );
}
